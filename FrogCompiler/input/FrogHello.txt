/* Frog Example: The Pond Program
   This program is written in the Frog language.
   It should hop through the scanner without any lexical errors */
tadpole main(tadpole x, tadpole y) {
	tadpole count;
	lilypad price;
	croak greeting;
	print("Ribbit! Hello from the pond!");
	count = 42;
	price = 3.14;
	count + count;
	count - count;
	count * count;
	count / count;
	count > count;
	count < count;
	count == count;
	count != count;
	count && count;
	count || count;
	!count;
	do {
		count;
	} hop count > 0;
	leap 0;
}
